type MyNumber = number
let num: MyNumber = 10

type fieldType = {
  prop: string,
  value: any,
  isShow?: boolean,
}

const fieldData: fieldType = {
  prop: 'name',
  value: 'why',
  isShow: true
}

const fieldData2: fieldType = {
  prop: 'name',
  value: 'why',
}